const userRole = {
    ADMIN: 'admin',
    VIEWER: 'viewer',
};

const userStatus = {
    ACTIVE: 'active',
    DISABLED: 'disabled',
    DELETED: 'deleted',
};

module.exports = {
    userRole,
    userStatus,
}
